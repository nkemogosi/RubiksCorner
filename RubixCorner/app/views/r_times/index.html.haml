%h1 Listing r_times

%table
  %thead
    %tr
      %th Minutes
      %th Seconds
      %th Millisecs
      %th Dnf
      %th Plus2
      %th
      %th
      %th

  %tbody
    - @r_times.each do |r_time|
      %tr
        %td= r_time.minutes
        %td= r_time.seconds
        %td= r_time.millisecs
        %td= r_time.dnf
        %td= r_time.plus2
        %td= link_to 'Show', r_time
        %td= link_to 'Edit', edit_r_time_path(r_time)
        %td= link_to 'Destroy', r_time, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New R time', new_r_time_path
